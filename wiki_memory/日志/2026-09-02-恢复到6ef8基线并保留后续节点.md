---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-02
topic: restore-6ef8-baseline
source_logs: []
supersedes: null
---

# 恢复到 6ef8 基线并保留后续节点

## 目标

将当前 `main` 的工作树恢复到 `6ef8b684aba137fd3aff7de026629e902ad6ad6c` 的项目基线，同时保留该节点之后已经产生的所有开发提交和历史关系。

## 执行方式

- 确认目标节点是当前 `main` 的祖先。
- 使用反向恢复提交还原目标节点之后的跟踪文件，没有使用 `reset --hard`、删除提交或强制推送。
- 保留本机 `Source/` 源模型与参考视频的忽略规则，避免恢复过程中误提交原始素材。

## 结果

- 当前工程代码、页面、166 帧兰花资产、清单和历史记忆回到 6ef8 基线内容。
- 后续蝴蝶切片、Blender 式三维视口等提交仍保留在当前提交的祖先历史中，可从 `a42bcc89b457bb1b376a3333853b86dc29ebc565` 等节点继续取回。
- 本次恢复本身以新提交落在 `main`，因此远程历史没有被改写。

## 验证

- 暂存区跟踪树与目标节点逐项比较一致；仅保留当前必要的本地 `Source/` 忽略规则和本次恢复日志。
- 后续完成 `npm run build`、`git diff --check` 和记忆 lint 后提交并推送 `origin/main`。

## 待确认长期记忆

- 目标节点恢复应使用新提交保留后续开发历史，不使用破坏性分支重置。

