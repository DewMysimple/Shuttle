---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-02
topic: restore-ed16-baseline
source_logs: []
supersedes: null
---

# 恢复到 ed16 基线并保留后续节点

## 目标

将当前 `main` 的工程恢复到 `ed16ade9d9b438f435672f1f579f06b892cf3fc1` 的内容基线，同时不删除该节点之后已经产生的开发提交和历史关系。

## 执行方式

- 确认 `ed16...` 是当前 `main` 的祖先节点，目标内容为兰花 166 帧时间切片基线。
- 中止一次误将目标哈希当作单个提交的未完成 revert，随后使用目标节点与当前 `HEAD` 的完整二进制差异反向应用。
- 没有使用 `reset --hard`、删除提交、删除本机源素材或强制推送。
- 保留本机 `Source/` 源模型/参考视频和 Python 缓存的忽略规则，避免恢复旧节点时污染提交范围。

## 结果

- 当前跟踪代码、页面、166 帧运行资产、清单和工程记忆回到 `ed16...` 的状态。
- `6ef8...`、`270518...` 以及其后的蝴蝶切片和三维视口开发节点仍在 Git 历史中保留，可继续对比或恢复。
- 本次恢复将通过新提交落在 `main`，远程历史不被改写。

## 验证

- 暂存区跟踪树与 `ed16...` 逐项比较一致；仅额外保留本地素材忽略规则和本次恢复日志。
- 完成 `npm run build`、`git diff --check` 和记忆 lint 后提交并推送 `origin/main`。

## 待确认长期记忆

- 回到历史节点时采用新恢复提交保留后续开发历史，不使用破坏性分支重置。

