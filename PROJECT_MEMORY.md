# 工程记忆：兰花 3D 时间切片

> 更新时间：2026-08-31
>
> 这是本项目的长期上下文、技术决策和执行记录。后续继续开发时，优先阅读本文，再查看 `README.md`、`src/main.js` 和 `scripts/export_blender_assets.py`。
>
> 结构化快速交接摘要同步维护在 `wiki_memory/`；每次代码、视觉、运行资产或记忆修改都必须同步更新相关记忆。

### 2026-08-31 最近修改

- 针对播放自然化后纵深穿越感变弱的问题，确认不应恢复整组切片沿 Z 轴高速位移；改为在固定时间隧道中增加低透明度 `playbackCursor`，让时间位置沿深度连续穿过，并将 `playbackTrail` 扩展为横向 + Z 轴残影，同时提高主焦点和环境通道光晕的局部强度。光晕用于连接空间层级，不用于整体提亮；未修改 PNG、GLB 或 Blender 源材质。
- 修复最大缩放时主体持续偏下的构图问题：诊断确认 PNG 主体本身接近画布中心，真正原因是相机 `lookAt` 高度使用 `y = 0.5`，高于中心 `focusLayer` 所在平面；运行时将 `CAMERA_FOCUS_Y` 校正为 `0.04`，并保持相机眼睛与焦点的原有相对高度差。未移动 PNG 或修改 Blender 源材质。
- 根据播放时“切片感太重”的反馈，重构网页运行时的时间表现：166 张时间切片固定在以时间中点为基准的世界 Z 轴空间中，播放不再推动整组切片高速移动；新增固定视觉中心的 `focusLayer`，用当前/下一张 PNG 按 `frameFloat` 小数部分连续交叉淡化，并在播放中加入最多 5 层低透明度轨迹残影。播放期间卡片结构与远端切片降噪，暂停后平滑恢复时间轴展示；保留长按、按钮、滚轮缩放和拖拽旋转，不新增滚动驱动时间帧，也未修改 PNG、GLB 或 Blender 源材质。
- 根据主时间切片的色彩反馈，仅在网页运行时强化焦点表现：当前帧图像透明度提高到约 `0.99`，相邻、中距离和远距离切片进一步压低；主帧使用约 `1.04–1.90` 的连续饱和度、最高约 `1.29` 的对比度和暖珊瑚色偏移，并增加轻微放大、轮廓和 `0.074 ± 0.018` 暖珊瑚微光。CSS 同时降低白色 screen 雾层并增加主体背后的低强度深玫红对比区；未修改 PNG、Blender 材质或交互。
- 根据焦点突出反馈，重新拉开当前帧与其他时间切片的视觉层级：当前帧图像透明度提高到约 `0.96`，相邻、中距离和远距离切片依次降至约 `0.16–0.30`、`0.06–0.14`、`0.02–0.06`；当前帧同时获得更高的饱和度、对比度和暖粉色加法微光。
- 微光以当前 `frameFloat` 的连续焦点权重计算，播放时相邻整数帧平滑过渡，不会跳光；增加约 `1.7s` 的轻微呼吸，`prefers-reduced-motion` 下保持静态焦点光。未新增后处理或运行资产。
- 恢复完整时间轨迹：播放推进到任意帧后，当前帧之前的历史切片仍保持可见，不再按 `index < floor(frameFloat)` 设为不可见；它们与当前帧及后续帧一起参与距离透明度分级。
- 主界面右侧新增“正放”“倒放”“开头”“结尾”四个按钮：正放/倒放单击开始对应方向播放，再次点击当前方向暂停；开头/结尾单击暂停并定位到首帧或尾帧。
- 根目录 `AGENTS.md` 新增 Git 提交回报约定：每次推送后必须在聊天中报告完整 40 位 commit hash、提交信息、`origin/main` 和工作区状态。
- 重新导出 166 张 PNG 前扫描完整动画包围盒，自动缩小并居中 Blender 相机；最终投影范围约为画面的 8%–92%，所有帧透明内容均不再触碰画布边缘，避免展开后的兰花被卡片裁剪。
- 将最大放大距离调整为 `3`，并提高俯仰范围、视角旋转灵敏度、旋转跟随和滚轮缩放响应，让主体可以接近参考图的聚焦尺度，同时保持当前时间切片为缩放焦点。
- 根据最新反馈，将时间切片从共面的 X 轴排列改为保持正面平行、沿卡片法线的世界 Z 轴以 `SLICE_DEPTH_STEP = 0.62` 等距分层；切片不再共面、相交、弯曲或逐片倾斜，统一 `rotationRig` 仍负责完整视角旋转。
- 根据最新交互要求，将俯仰边界扩大到 ±90°，保持向下拖动俯视、向上拖动仰视；同时恢复历史切片可见，所有 166 帧都继续参与渲染。
- 根据最新交互要求，将平行切片相邻深度步进提高到 `1`；视角拖拽响应提高为每个视口宽度约 `396°`、每个视口高度约 `120°`，拖拽/释放阻尼提高为 `18/10`。
- 修复收拢态整组切片消失的问题：收拢时以当前 `frameFloat` 四舍五入后的切片为唯一清晰基准，其余重叠切片淡出；展开时恢复完整时间轨迹。

### 最近修改

- 2026-08-30：继续调整视角交互。俯仰响应提高为每个视口高度约 84°，限制放宽为 ±36°；横向每个视口宽度约旋转 1.6π（288°），拖拽跟随阻尼提高到 11；仍保持向下拖动俯视、向上拖动仰视，不允许翻转失控。
- 同次交互微调将滚轮最近相机距离从 5.8 降为 5.2，让主体可以再靠近一点；缩小边界 15.5 和缩放阻尼保持不变，放大焦点跟随当前时间切片的连续中心。
- 2026-08-31：根据主体聚焦反馈，将俯仰响应提高到每个视口高度约 100°、范围扩大到 ±45°；横向每个视口宽度约旋转 1.85π（333°），拖拽和释放跟随加快为 14/8；滚轮缩放阻尼和灵敏度提高为 11/0.011，最近相机距离降为 3。
- 同次修改允许长按播放期间继续拖拽旋转视角；左键长按正向播放，右键长按倒放，播放与 yaw/pitch 分属独立状态，松开指针只暂停播放。
- 2026-08-30：将主界面时间控制改为长按播放机制。按住画布超过约 260ms 后以 30fps 播放全部 166 张时间切片，松开立即暂停，播放到末帧后停留，再次长按从首帧开始。
- 同次修改将滚轮职责限定为主体缩放：向上滚轮拉近相机、向下滚轮拉远相机，使用 5.2–15.5 的距离边界和可打断阻尼，不改变时间帧或旋转角度；放大时相机对准当前时间切片的连续中心，页面改为单屏舞台，不再用滚动驱动时间。（后续最近距离已调整为 3；当前切片布局已改为直线。）
- 长按和旋转采用 8px 分流死区：未触发长按前移动超过死区进入双轴拖拽；播放期间仍可旋转，保留向下俯视、向上仰视、连续 360° 以上 yaw 和丰富环境动效。

- 2026-08-30：按视觉稿收敛为单一主界面。删除独立引导页、结尾说明页和画布中央的额外提示，只保留固定舞台、时间切片、帧数信息、拖拽提示和展开按钮。
- 同次修改将主界面的滚动区调整为 520vh，继续保留滚动逐帧和水平拖拽交互（历史版本）。
- 验证结果：页面只包含 1 个主 section；首屏状态为 `FRAME 01 / 76`；滚动到中段可到 `FRAME 47 / 76`；展开按钮可切换为“收拢切片”；桌面端 1440×900 首屏加载完成后 loading 面板自动隐藏。
- 2026-08-30：主体模型替换为 `兰花_形态1.blend`。该文件使用 1–166 帧的 Alembic MeshSequenceCache，不是骨骼 Action；导出脚本现在会生成 76 个 morph targets 和对应动画，再导出 GLB 与 PNG 切片。
- 兰花缓存路径已确认存在于外部素材库：`C:\Users\Administrator\Desktop\Verminoble\blender_scenebench\blender_modelbench\兰花\形态1\alembic\alembic\OrchidMeshGrp.abc`。缓存约 106 MB，仅用于重新导出，不提交到网页仓库。
- 本次替换验证：GLB 包含 77 个 shape keys（Basis + 76 帧）和 `Orchid_Time_Slices` 动画；重新生成 76 张 PNG；网页标题、加载文案和主界面标签均已改为兰花。
- 2026-08-30：移除之前为贴近参考图而额外生成的棕色展示台。当前 GLB 只包含兰花 morph animation，页面不再显示圆台；时间切片本身也只渲染兰花。
- 2026-08-30：增强画布视角交互。横向拖拽现在使用不封顶的方位角累积，兰花可以连续旋转完整 360° 及更多圈；纵向拖拽控制带俯仰上限的上下视角。展开按钮不再重置视角，`R` 可恢复默认视角。
- 同次修改增加主体呼吸漂浮、时间切片错位漂移、环境粒子、轨道光环、镜头微动和渐变雾光流动，让主界面动效更丰富；`prefers-reduced-motion` 仍会降低或关闭持续运动。
- 2026-08-30：修复 `runStart.cmd` 的批处理目录变量错误，将 `%~dp` 改为 `%~dp0`；现在双击脚本会正确进入工程目录，检查 Node.js/npm，必要时自动安装依赖并启动 Vite。
- 2026-08-30：按眩晕反馈重构旋转交互。横向拖拽只累积 `yawTarget`，纵向拖拽只调整受限的 `pitchTarget`；拖拽超过约 8px 后进入双轴控制，展开切片改由按钮独立控制。
- 同次修改将兰花与 76 张时间切片放入统一 `rotationRig`，固定相机不再随用户视角绕行，移除滚动后段自动镜头旋转和多层视差叠加；保留低幅度环境与主体动效。
- 2026-08-30：根据交互反馈修正俯仰语义：屏幕向下拖动增加俯视角、向上拖动减少俯视角（对应仰视）；键盘上下方向键与该语义保持一致。
- 同次修改将旋转阻尼从单一高响应系数改为拖拽跟随 9、松手收敛 7 的两档可打断阻尼；不增加惯性，避免角度追随过硬或释放后继续甩动。
- 2026-08-30：按“只留下时间切片”的要求移除网页运行时的 GLB 加载、AnimationMixer 和中央实时兰花。`rotationRig` 现在只承载 76 张透明 PNG 时间切片；切片默认以 0.72 的中等展开度显示，按钮仍可收拢或完全展开。
- 同次修改将加载流程改为只请求 `slice-manifest.json` 和 76 张 PNG，`flower.glb` 仅作为可选导出检查资产保留，不再参与页面渲染；切片组保留独立漂浮、错位、粒子、光环和渐变雾光动效。
- 2026-08-30：按参考图完成“时间隧道”切片布局。切片位置改为相对 `frameFloat` 的当前帧中心锚定，卡片调整为 2.15×1.72、图片调整为 1.92×1.54，X/Z 间距改为 0.1/0.18；正面形成嵌套隧道，侧面形成有间距的时间序列。
- 同次修改建立中心帧、邻近帧、中距离帧和远端帧的透明度层级，并用视角相关的尾部增强和 WebGL 饱和度/对比度分级突出杜鹃花；CSS 白色 screen 雾层降至 0.12。
- 交互偏好：以丰富、明显的空间动效和旋转反馈为主，不把 Apple Design 的弱化动效取向作为本项目约束；仅保留系统 `prefers-reduced-motion` 作为无障碍兜底。
- 工程约定：每次代码或视觉修改都必须同步更新本文，完成独立 Git 提交并推送到 `origin/main`。
- 2026-08-30：根据“包含全部帧提取”的要求，将导出目标从 76 个均匀采样状态改为源动画的全部整数帧；当前兰花场景范围为 1–166，共生成 166 张透明 PNG，网页切片数量改由 `slice-manifest.json` 动态决定。
- 同次修改将时间隧道相邻切片的空间步进提高为 X `0.16`、Z `0.30`，让侧面切片轮廓和正面隧道层次保持更清楚的距离。

## 1. 项目目标

制作一个独立的 Three.js/Vite 互动作品页，主体是兰花，不是参考视频中的蝴蝶。

核心体验：

- 长按画布播放兰花形态动画的 166 张透明渲染切片，松开暂停。
- 动画按源场景 1–166 的每个整数帧提取，并由 PNG 切片直接呈现。
- “展开切片”按钮让 166 张透明时间切片沿世界 Z 深度轴展开或收拢。
- 展开后能看到连续的 3D 平行切片序列、嵌套层次、独立卡片边缘和视角变化。
- 当前帧固定在视觉中心；固定时间轴负责保留 166 张切片的空间结构，播放由独立 `focusLayer` 的当前/下一帧连续交叉淡化、沿固定深度穿过的低透明度时间游标、横深轨迹残影和局部通道光晕表现，不再让整组切片沿深度轴高速扫过镜头。
- 滚轮只控制主体的相机距离缩放，不参与时间播放、切片展开或旋转；相机缩放始终以视觉中心焦点为目标，最大放大时主体不应持续落在画面下方。
- 粉色、紫色、黄色的粉彩雾光和透明卡片构成整体视觉。
- 桌面端优先保证完整空间效果，移动端自动降低 DPR、切片组尺寸和环境细节。

参考视频的重点不是复用蝴蝶素材，而是吸收其稳定主体、连续姿态过渡和时间切片构图；本项目不新增滚动驱动时间帧，时间仍由按钮和长按自动播放控制，当前页面不显示额外的实时 3D 主体。

## 2. 源素材与已确认事实

### Blender 源文件

实际使用的源文件是：

`C:\Users\Administrator\Desktop\Free\兰花_形态1.blend`

当前使用的实际路径是 `C:\Users\Administrator\Desktop\Free\兰花_形态1.blend`。曾输入过的 `C:\Users\Administrator\Desktop\Free\兰花\_形态1.blend` 不存在；兰花源文件位于本工程目录外，不进入 Git 仓库。

使用 Blender：

`F:\Blender\blender.exe`

Blender 检查结果：

- 场景：`兰花_形态1_模型展示`
- 帧范围：1–166
- 帧率：30 fps，约 5.5 秒
- 动画对象：`兰花_形态1_动画缓存`
- 动画类型：MeshSequenceCache，使用 `OrchidMeshGrp.abc`
- 缓存网格：8502 vertices、8488 polygons
- 三组静态源形态：闭合、半闭合、开放
- 当前不导出展示台，GLB 和时间切片只包含兰花主体
- Blender 相机本身没有动画
- Blender 导出脚本会在渲染 PNG 前按完整动画评估包围盒自动调整镜头焦距和 shift，保持原相机方向并为最宽姿态保留安全边距；该设置不会写回源 `.blend`
- 4 张兰花贴图已打包在 `.blend` 中，尺寸为 1024×1024
- Blender 文件没有 Actions 或 Shape Keys；导出脚本仍可烘焙 morph targets 供 GLB 检查，但网页当前只使用透明 PNG 时间切片

外层素材目录 `C:\Users\Administrator\Desktop\Free\形态1` 还包含原始 FBX、MAX 和贴图，但网页导出不依赖它们。

### 参考 MP4

参考视频位于外层目录，约 3444×1936、17 秒、约 515 帧。视频概念上将动作组织为时间切片；本次复盘确认参考片段不是网页滚动播放器，因此只借鉴其主体稳定、连续过渡和残影关系，不把滚动控制接回 `frameFloat`。网页端继续使用固定相机转台响应用户拖拽旋转。

## 3. 已确定的技术方案

当前网页采用切片优先方案：

1. `frame-001.png` 至 `frame-166.png`：Blender 按源动画每个整数帧透明渲染得到的 166 张时间切片，用于形成可展开的空间轨迹，也是网页的唯一主体视觉资产。
2. Three.js：负责透明纹理、切片卡片、相机、统一转台和交互；不加载实时 GLB，不创建 `AnimationMixer`。
3. `flower.glb`：导出脚本生成的可选检查资产，保留在仓库中但不被主界面请求。
4. `focusLayer`：负责固定在视觉中心的当前/下一帧交叉淡化、主焦点微光与播放轨迹残影；固定时间轴切片只负责展开后的空间结构。
5. CSS：负责页面渐变、雾光、信息层、布局和移动端适配。

当前兰花时间采样规则（源动画 1–166 帧）：

```js
sampleFrame(i) = i + 1, i ∈ [0, 165]
```

页面状态由以下数据驱动：

```ts
type TimeSliceState = {
  frameFloat: number;     // 0–165
  spread: number;         // 0–1
  yaw: number;
  pitch: number;
  isPlaying: boolean;
  playbackDirection: 1 | -1;
}
```

交互映射：

- 左键长按超过约 260ms → `playbackDirection = 1` 正向播放；右键长按 → `playbackDirection = -1` 倒放；均以 30fps 推进 `frameFloat`，松开或到达对应边界暂停。
- 右侧“正放”“倒放”按钮单击启动对应方向，再次点击当前播放按钮暂停；“开头”“结尾”按钮暂停并将 `frameFloat` 定位到 `0` 或 `sliceCount - 1`。
- 所有时间轴切片均保持 `visible = true` 并参与固定空间布局；`timelineCenter = (sliceCount - 1) / 2`，位置为 `slice.position = (0, 0, -(index - timelineCenter) × 1 × spread)`，播放不再改变整组切片的位置。播放期间固定时间轴整体降噪，当前/下一帧由 `focusLayer` 在视觉中心连续交叉淡化，最多 5 层 `playbackTrail` 以低透明度模拟运动轨迹；暂停后轨迹平滑退出，收拢态仍由主焦点层保证当前帧可见。
- 主焦点由透明度、运行时饱和度/对比度、暖珊瑚色偏移、轻微尺度提升和低强度加法微光共同突出；微光约为 `0.074 ± 0.018`，按 `frameFloat` 连续过渡，`prefers-reduced-motion` 下保持静态。固定时间轴的卡片轮廓只在非播放状态承担时间序列表达，避免播放时规则矩形叠层扩散焦点。
- 滚轮 → 相机距离目标值 → 平滑缩放主体，距离范围为 3–15.5；相机保持低幅度环境焦点，不再随 `frameFloat` 漂移，不修改 `frameFloat`、`yaw`、`pitch` 或 `spread`。
- 展开按钮 → `spreadTarget`，控制平行切片沿世界 Z 深度轴等距展开；拖拽不再改变展开状态。
- Pointer Events 拖拽超过约 8px 起拖死区后，横向增量独立累积不封顶的 `yawTarget`，纵向增量按“下拖俯视、上拖仰视”独立映射到 ±90° 范围内的 `pitchTarget`；斜向拖动同时改变两个角度，可连续绕 Y 轴 360° 旋转；播放期间同样消费拖拽增量。
- `rotationRig` 统一旋转时间切片，固定相机不随用户视角移动；拖拽时使用 18、松手时使用 10 的更快短阻尼过渡，不产生松手惯性；横向每个视口约旋转 396°，纵向每个视口约响应 120°并限制在 ±90°。
- 时间切片和环境粒子仍有低幅度漂浮与呼吸动效，但播放和滚轮不触发自动镜头绕行。
- `sideView = abs(sin(yaw))` → 侧面观察时提高非焦点切片尾部可见度，正面则优先保持中心主体清晰。
- 键盘支持方向键、Home、End，`R` 恢复视角，展开按钮支持无鼠标访问。
- `prefers-reduced-motion` 下关闭镜头旋转、弹性动画和焦点微光呼吸，仅保留静态焦点光。

## 4. 工程结构

```text
rhododendron-time-slices/
├─ index.html                         页面结构和作品文案
├─ src/
│  ├─ main.js                         Three.js 场景、动画和交互
│  └─ style.css                       粉彩视觉、布局、响应式样式
├─ scripts/
│  └─ export_blender_assets.py        Blender Alembic→GLB/切片自动导出脚本
├─ public/assets/
│  ├─ flower.glb                       可选导出检查资产，主界面不加载
│  ├─ slice-manifest.json              166 帧清单
│  └─ slices/frame-001.png … 166.png  透明时间切片
├─ package.json
├─ package-lock.json
├─ README.md
└─ PROJECT_MEMORY.md
```

## 5. 已执行过程

1. 检查外层文件，确认实际 `.blend` 路径和 Blender 可执行文件。
2. 用 Blender 只读检查场景、对象、动画、骨骼和贴图。
3. 确认参考视频后段确实包含 3D 场景旋转和时间切片视觉。
4. 创建 Vite + Three.js 独立工程。
5. 编写 Blender 自动处理脚本，将兰花 Alembic 缓存烘焙为 morph targets，导出可选 `flower.glb`，并按源动画整数帧生成透明 PNG。
6. 实现实时 GLB 动画、时间切片、滚动映射、切片展开和自由视角旋转；后续按视觉要求移除网页中的实时 GLB 层。
7. 完成粉彩渐变、透明卡片、雾光、响应式和 reduced-motion 处理。
8. 执行 `npm install` 和 `npm run build`，构建成功。
9. 启动本地 Vite 服务，在桌面、移动端和 reduced-motion 场景下进行浏览器验证。
10. 验证 manifest 和全部 166 张 PNG 均可访问，网页不请求 GLB，旧版滚动和拖拽状态曾可正常更新。
11. 验证正面存在嵌套时间隧道，侧面存在独立切片间距，当前杜鹃花颜色不会被透明叠加洗白。
12. 将时间控制切换为长按播放、松开暂停，将滚轮切换为主体缩放，并保留拖拽旋转与按钮展开的独立职责。
13. 将左键/右键长按分别映射为正向/倒放，并允许播放期间旋转视角；俯仰灵敏度提高、上限放宽为 ±36°。
14. 增加右侧单击播放与边界跳转控制，并在播放过程中隐藏当前帧之前已经越过相机视野的切片。
15. 发现原 Blender 相机只覆盖闭合前段，补充全动画包围盒相机适配；重新渲染 166 张 PNG，闭合、展开中和完全展开代表帧均不再触碰透明画布边缘。

## 6. 已知限制与处理方式

- 兰花源文件不是骨骼动画，而是依赖外部 Alembic MeshSequenceCache。导出脚本已将每个采样帧烘焙为 shape key，并建立 `Orchid_Time_Slices` morph animation；重新导出时必须提供 `OrchidMeshGrp.abc`。
- Alembic 缓存约 106 MB，不进入 Git 或网页运行时；如果换机器，需要重新提供缓存路径。
- Blender 原相机无动画；当前网页使用固定相机与统一转台实现视角旋转，不依赖 Blender 相机动画。
- 源相机原始构图无法覆盖兰花完全展开后的姿态；导出脚本通过扫描所有评估帧，调整焦距与镜头 shift，将联合投影范围限制在约 0.08–0.92。若未来更换模型或动画，必须重新执行该安全构图检查。
- 当前网页不加载 `flower.glb`，只使用 166 张透明 PNG；如果未来再次启用实时模型，需要重新建立独立的运行时决策并评估首屏性能。
- 播放中的历史切片不是被删除或隐藏；它们始终保留在 Three.js 渲染树中，并按距离透明度参与完整时间轨迹。扩大到 ±90° 的俯仰意味着接近极点时会出现正常的极限视角变化，边界限制仍阻止超出范围。
- 时间切片使用 RGBA PNG，而不是直接使用视频帧，以保留透明边缘并避免把 3444×1936 的视频帧全部带入网页。
- 目前没有加入重型后处理 Bloom，主要使用渐变、透明材质、当前帧加法微光、边缘线、雾光、粒子和轨道光环完成视觉；如性能允许，后续才评估桌面端轻量 Bloom。
- 旧 WebP 实验文件不被网页引用，统一排除，不作为运行资产。

## 7. 日常运行与重新导出

在工程目录执行：

```powershell
npm install
npm run dev
```

重新从 Blender 生成运行资产：

```powershell
& 'F:\Blender\blender.exe' --background --factory-startup 'C:\Users\Administrator\Desktop\Free\兰花_形态1.blend' --python '.\scripts\export_blender_assets.py' -- "$PWD\public\assets" 'C:\Users\Administrator\Desktop\Verminoble\blender_scenebench\blender_modelbench\兰花\形态1\alembic\alembic\OrchidMeshGrp.abc'
```

生产构建：

```powershell
npm run build
npm run preview
```

## 8. 验收基线

提交或部署前至少确认：

- `npm run build` 成功。
- `slice-manifest.json` 与 166 张 PNG 没有 404；网页不应请求 `flower.glb`。
- 重新导出后所有 PNG 的 Alpha 包围盒都不能触碰 768×768 画布边缘；至少检查首帧、中段和尾帧，并确认主体位于卡片安全区域内。
- 左键长按从当前帧正向播放，右键长按从当前帧倒放，松开立即暂停，到达对应边界后停留，再次长按从相应起点重新播放。
- 滚轮向上放大、向下缩小主体，缩放响应更快、仍保持平滑且不改变当前帧、旋转和展开状态；最近距离 3 时达到参考图的主体聚焦尺度，同时继续以当前时间切片为焦点。
- 切片可以完全收拢、展开，并支持中途反向拖拽；完全收拢时当前时间切片仍保持可见。
- 画布横向拖拽可连续旋转 360° 及更多圈，纵向拖拽限制在 ±90°且响应更灵活；双轴反向操作不会跳变，斜向拖动仍能同时调整视角。
- 右侧“正放”“倒放”单击可以开始/暂停对应方向播放；“开头”“结尾”单击后停在相应边界，播放时当前帧之前的历史切片仍可见。
- 向下拖动进入俯视、向上拖动进入仰视；拖拽和键盘方向语义一致，旋转跟随柔和且松手不甩动。
- 展开按钮不会改变当前旋转视角，长按播放和滚轮缩放不会触发自动镜头绕行；滚轮放大以当前时间切片为焦点。
- 展开后有明显的直线空间间距、遮挡和时间顺序；旋转到侧面时可辨认连续的平行切片边缘。
- 单屏画布可用，移动端长按播放和双轴拖拽不会被浏览器默认手势抢占；播放期间继续拖拽可改变视角。
- reduced-motion 下不会持续自动绕行或弹性运动。
- 推送远程后，干净克隆可以直接安装依赖并启动页面。

## 9. Git 约定

- Git 根目录：本目录。
- 默认分支：`main`。
- 远程：`https://github.com/DewMysimple/Shuttle.git`。
- 每次提交并推送后，聊天回复必须报告完整 40 位 commit hash、提交信息、远程 `origin/main` 和工作区是否干净。
- 第一条提交信息：`feat: add rhododendron 3d time slice experience`。
- 不提交外层 `.blend`、MP4、FBX、MAX 和原始贴图目录。
